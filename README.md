# Dev command below

`npm install`

`npm run dev`

`composer update`

`cp .env.example .env`

`php artisan key:generate`

`php artisan migrate --seed`

`php artisan storage:link`

`php artisan serve`

## Production command

`npm run build`
